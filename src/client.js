/* const io = require('socket.io-client')
const socket = io('http://localhost:3000')
var string = 'nothing'

socket.on('connect', () => {
  socket.on('ok', () => console.log('all good'))
})

const KEY = {
  W: 87,
  A: 65,
  S: 83,
  D: 68
} */

/* document.addEventListener('keydown', function (e) {
  switch (e.keyCode) {
    case KEY.S: string = socket.emit('string', 'Down!')
    case KEY.S: string = socket.emit('string', 'Up!')
    case KEY.S: string = socket.emit('string', 'Left!')
    case KEY.S: string = socket.emit('string', 'Right!')
  }
})
*/
